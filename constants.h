#pragma once

#include <string>
#include <filesystem>

const std::filesystem::path DATA_PATH = std::filesystem::current_path() / "";
const std::string METADATA_EXT = ".meta";
const std::string STORAGE_EXT = ".dat";